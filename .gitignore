# Python
__pycache__/
*.pyc
*.egg-info/
.pytest_cache/

# Server/release data NEVER goes in git. It all lives one level up, outside
# this repo (data_dir defaults to ".."). These entries are a safety net in
# case data is ever copied into the repo or data_dir is pointed at ".".
Output/
SOQL Exports/
00. Previous Release/
01. Current Release/
Apttus Files/
Split/
Validation/
*.xlsx
*.xls
*.csv
